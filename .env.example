# G²AENome Environment Variables Example

# OpenAI API Key (optional, if using OpenAI models via LangChain)
OPENAI_API_KEY=""

# HuggingFace API Token (optional, if needed for gated models or private hub access)
HUGGINGFACE_API_TOKEN=""

# MLflow Tracking URI (optional, defaults to local ./mlruns if not set)
# MLFLOW_TRACKING_URI="http://localhost:5000"

# Add other environment-specific configurations here
# For example, database connection strings if using a persistent KG/Vector DB
# POSTGRES_USER=""
# POSTGRES_PASSWORD=""
# POSTGRES_DB=""
# POSTGRES_HOST=""
# POSTGRES_PORT=""
